import React from 'react';
import { Helmet } from 'react-helmet';
import { SEOProps } from '../models/components/seo.model';

export default function SEO({ title, page, description, image, where }: SEOProps) {

}